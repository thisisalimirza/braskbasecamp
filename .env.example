# Turso database (leave unset to use a local SQLite file at ./local.db)
# TURSO_DATABASE_URL=libsql://your-db.turso.io
# TURSO_AUTH_TOKEN=

# Session cookie signing secret. REQUIRED in production.
# Generate with: openssl rand -base64 32
SESSION_SECRET=
